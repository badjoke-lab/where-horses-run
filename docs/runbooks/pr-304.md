# PR #304 — publication QA for entries 29–36

Status: GitHub pre-preview QA in progress  
Normal work branch: `country-pages-29-36-publication-qa`  
Final preview branch: create or move one `preview-*` branch only after GitHub QA passes  
Production publication: prohibited until rendered preview review passes

## Scope

Validate the English and Japanese country routes for:

- United Kingdom
- United States
- Australia
- Ireland
- France
- Canada
- Saudi Arabia
- India

## Reviewed public ceilings

- A: Ireland, France, Saudi Arabia
- C: United Kingdom, United States, Australia, Canada, India

Subsystem-level schedule capability must not raise the country ceiling where national coverage remains incomplete.

## GitHub QA gate

The repository must verify:

- all sixteen built routes exist
- one h1 per route
- page-specific canonical links
- reciprocal English and Japanese hreflang links
- language switching to the corresponding country route
- reviewed official source links are rendered
- C pages do not render start-time or timezone meeting columns
- A pages do not exceed the reviewed A boundary
- no embedded video or `Watch here` output
- empty meeting data is not described as absence of racing
- runtime remains Profile v2 only
- entries remain at least `profile_ready` until rendered preview approval

## Validation

```text
npm install
npm run build
node scripts/check-country-page-publication-29-36.mjs
node scripts/check-country-profiles-29-36.mjs
node scripts/check-country-detail-profile-runtime.mjs
node scripts/check-country-page-programme.mjs
```

## Preview gate

After GitHub QA is green:

1. Create or move one final `preview-*` branch to the reviewed PR head.
2. Allow exactly one Cloudflare preview deployment.
3. Review representative A and C pages in English and Japanese.
4. Check responsive layout, official links, language switching, and empty states.
5. Only after approval, advance entries 29–36 to `published`, set `qa_status=passed`, and add `page_published_at`.
6. Merge without `[CF-Pages-Skip]` and confirm one production deployment.

## Current non-publication state

Until rendered preview approval, entries 29–36 remain `profile_ready` with complete bilingual routes and no publication date.

PR #300 remains the separate publication gate for entries 21–28.
