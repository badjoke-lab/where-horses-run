# PR #300 — publication QA for entries 21–28

Status: rendered preview found public-copy issues; remediation in progress  
Normal work branch: `country-pages-21-28-publication-qa`  
Final preview branch: `preview-country-pages-21-28`  
Production publication: prohibited until the corrected rendered preview passes

## Scope

Validate the English and Japanese routes for Hungary, Malta, Austria, Puerto Rico, Jamaica, Trinidad and Tobago, Barbados, and Martinique.

## Preview findings

The first rendered preview succeeded technically but exposed two internal-only strings:

- `Verify the current local UTC offset`
- implementation wording about replacing the list after the calendar data model is finalized

These strings must be removed before publication.

## GitHub QA gate

The repository verifies:

- all sixteen built routes exist
- one h1 per route
- canonical and reciprocal hreflang links
- language switching
- reviewed official-source links
- C pages omit start-time and timezone meeting columns
- empty meeting data is not presented as absence of racing
- internal implementation and editorial placeholder strings are absent
- runtime remains Profile v2 only

## Current tracker

```text
published:       20
profile_ready:   24
source_tested:    8
not_started:     46
total:           98
```

After preview approval, only entries 21–28 advance to published. Expected counts then become published 28, profile_ready 16, source_tested 8, and not_started 46.

The final merge must not use `[CF-Pages-Skip]` because it intentionally triggers one production deployment.
