# PR-063 — Wire Japan active timetable validator

## Summary

This PR wires the PR-062 Japan active timetable validator into the main validation chain.

## Change

- Adds `validate:japan-active-timetable-records` to `package.json`.
- Includes it in `npm run check` before UI and build checks.

## User-facing outcome

No direct UI change.

The Japan active-window timetable overlay introduced in PR-062 is now guarded by the project-level check command.

## Validation

Run:

```sh
npm run validate:japan-active-timetable-records
npm run check
```

## Next PR

PR-064 should continue actual active-window coverage expansion rather than adding status-only display.
