# PR-080: M3 v0 status documentation / release note

## Summary

PR-080 adds a concise internal/public-safe release note for the M3 v0 generator foundation status after the M3 v0 release gate.

The note records that the M3 v0 generator foundation is internally gated, public coverage remains partial, and no complete coverage claim is made.

## Files

- `docs/runbooks/m3-v0-status-release-note.md` — public-safe M3 v0 generator foundation status release note.
- `docs/runbooks/pr-080.md` — this PR runbook.
- `docs/runbooks/README.md` — adds a runbook index link to the new release note.
- `docs/runbooks/current-status.md` — links the release note and points the next phase beyond PR-080.

## User-facing outcome

Documentation only.

No data, generated overlays, UI features, live fetch, source parsing, or public coverage behavior changes are introduced.

## Safety boundary

This PR does not add:

- live fetch
- source page parsing
- raw source body storage
- racecards
- odds
- results
- payouts
- predictions
- tips
- public overlay replacement
- UI changes
- new dependencies

## Validation commands

Run these commands before merging:

```bash
npm run build
npm run check
```

## Next PR

PR-081 or the next roadmap item should continue from the post-M3 v0 roadmap without weakening the safety boundary.
