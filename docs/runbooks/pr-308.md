# PR #308 — publication QA for entries 37–44

Status: GitHub pre-preview QA in progress  
Normal work branch: `country-pages-37-44-publication-qa`  
Production publication: prohibited until rendered preview review passes

## Scope

Validate the English and Japanese routes for Malaysia, Thailand, Philippines, Mauritius, Argentina, Germany, Italy, and Spain.

All eight reviewed public ceilings remain C.

## GitHub QA gate

The repository verifies:

- all sixteen built routes exist
- one h1 per route
- page-specific canonical links
- reciprocal English and Japanese hreflang links
- corresponding language-switch routes
- reviewed official-source links
- no start-time or timezone meeting columns on C pages
- no embedded video or `Watch here` output
- safe empty-state wording
- Profile v2-only runtime
- tracker rows remain `profile_ready` and unpublished

## Validation

```text
npm install
npm run build
node scripts/check-country-page-publication-37-44.mjs
node scripts/check-country-profiles-37-44.mjs
node scripts/check-country-detail-profile-runtime.mjs
node scripts/check-country-page-programme.mjs
```

## Remaining preview gate

1. Create or move one final `preview-*` branch to the reviewed head.
2. Allow one Cloudflare preview deployment.
3. Review representative English and Japanese C pages.
4. Check responsive layout, official links, language switching, and empty states.
5. After approval, advance entries 37–44 to `published`, set `qa_status=passed`, and add `page_published_at`.
6. Merge without `[CF-Pages-Skip]` and confirm one production deployment.

Until preview approval, entries 37–44 remain `profile_ready` with no publication date.

PR #300 and PR #304 remain separate publication gates.
