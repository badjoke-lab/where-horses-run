---
import RacecourseDirectoryPage from '../../components/RacecourseDirectoryPage.astro';
import { getRacecourseFilterOptions, getRacecourseFilterRecords } from '../../lib/racecourse-filter-data';

const records = getRacecourseFilterRecords('en');
const options = getRacecourseFilterOptions(records);
---

<RacecourseDirectoryPage locale="en" records={records} options={options} />
