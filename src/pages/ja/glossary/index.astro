---
import GlossaryDirectoryPage from '../../../components/GlossaryDirectoryPage.astro';
import {
  getGlossaryCategoryOptions,
  getGlossarySearchRecords,
} from '../../../lib/glossary-search-data';

const records = getGlossarySearchRecords('ja');
const categories = getGlossaryCategoryOptions(records, 'ja');
---

<GlossaryDirectoryPage locale="ja" records={records} categories={categories} />
