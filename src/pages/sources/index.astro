---
import SourceDirectoryPage from '../../components/SourceDirectoryPage.astro';
import { getSourceFilterOptions, getSourceFilterRecords } from '../../lib/source-filter-data';

const records = getSourceFilterRecords('en');
const options = getSourceFilterOptions(records);
---

<SourceDirectoryPage locale="en" records={records} options={options} />
