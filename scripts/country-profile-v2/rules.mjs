export const PROFILE_VERSION = '2.0.0';
export const PROFILE_STATUSES = new Set(['draft', 'reviewed']);
export const PAGE_KINDS = new Set(['country', 'special', 'explanatory', 'archive']);
export const DISPLAY_CEILINGS = new Set(['A+', 'A', 'B+', 'B', 'C', 'pending']);
export const SOURCE_TEST_STATUSES = new Set(['complete', 'partial', 'pending', 'not_applicable']);
export const ACTIVE_PAGE_KINDS = new Set(['country', 'special']);
export const ID_PATTERN = /^[a-z0-9]+(?:-[a-z0-9]+)*$/;
