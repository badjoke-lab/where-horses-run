name: Temporary Actions multi-job diagnostic

on:
  pull_request:
    paths:
      - '.github/workflows/temp-actions-multi-job-diagnostic.yml'

permissions:
  contents: write

jobs:
  diagnose:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
      - uses: actions/setup-node@v4
        with:
          node-version: 22.12.0
      - name: Capture checker output
        run: |
          mkdir -p .diagnostics
          node scripts/check-calendar-actions-multi-job.mjs > .diagnostics/actions-multi-job-check.log 2>&1 || true
      - name: Commit diagnostic output
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .diagnostics/actions-multi-job-check.log
          git commit -m "[CF-Pages-Skip] Capture Actions multi-job diagnostic"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
