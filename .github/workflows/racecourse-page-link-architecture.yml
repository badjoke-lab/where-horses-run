name: Racecourse page link architecture

on:
  pull_request:
    paths:
      - 'data/audits/racecourse-page-link-architecture-v1.json'
      - 'data/static/glossary.json'
      - 'data/static/racecourse-link-amendments-v1.json'
      - 'data/static/racecourses*.json'
      - 'data/generated/timetable/public/**'
      - 'docs/racecourses/page-link-architecture.md'
      - 'scripts/check-racecourse-page-link-architecture.mjs'
      - 'src/components/RacecoursePublicMeetingPanel.astro'
      - 'src/lib/data.ts'
      - 'src/pages/tracks/**'
      - 'src/pages/ja/tracks/**'
      - '.github/workflows/racecourse-page-link-architecture.yml'
  push:
    branches:
      - main
    paths:
      - 'data/audits/racecourse-page-link-architecture-v1.json'
      - 'data/static/glossary.json'
      - 'data/static/racecourse-link-amendments-v1.json'
      - 'data/static/racecourses*.json'
      - 'data/generated/timetable/public/**'
      - 'docs/racecourses/page-link-architecture.md'
      - 'scripts/check-racecourse-page-link-architecture.mjs'
      - 'src/components/RacecoursePublicMeetingPanel.astro'
      - 'src/lib/data.ts'
      - 'src/pages/tracks/**'
      - 'src/pages/ja/tracks/**'
      - '.github/workflows/racecourse-page-link-architecture.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      WHR_CALENDAR_REFERENCE_DATE: '2026-07-14'
      WHR_CALENDAR_TIMEZONE: Asia/Tokyo
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.12.0
      - name: Validate checker syntax
        run: node --check scripts/check-racecourse-page-link-architecture.mjs
      - name: Install dependencies without lockfile mutation
        run: npm install --package-lock=false
      - name: Build deterministic bilingual site
        run: npm run build
      - name: Validate racecourse page-link architecture
        run: node scripts/check-racecourse-page-link-architecture.mjs
      - name: Validate read-only boundary
        run: |
          git diff --check
          changes="$(git status --porcelain --untracked-files=all)"
          if [ -n "$changes" ]; then
            printf '%s\n' "$changes"
            echo 'Racecourse page-link validation modified repository files.' >&2
            exit 1
          fi
