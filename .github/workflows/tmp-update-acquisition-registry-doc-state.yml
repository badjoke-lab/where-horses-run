name: Temporary Acquisition Registry documentation state update

on:
  pull_request:
    paths:
      - 'scripts/tmp-update-acquisition-registry-doc-state.mjs'
      - 'scripts/tmp-patch-acquisition-registry-validator.mjs'
      - '.github/workflows/tmp-update-acquisition-registry-doc-state.yml'
  push:
    branches:
      - work/calendar-acquisition-registry

permissions:
  contents: write

concurrency:
  group: tmp-update-acquisition-registry-doc-state
  cancel-in-progress: false

jobs:
  update-and-clean:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: work/calendar-acquisition-registry
      - uses: actions/setup-node@v4
        with:
          node-version: 22.12.0
      - name: Apply exact documentation transitions
        run: node scripts/tmp-update-acquisition-registry-doc-state.mjs
      - name: Align validator with implemented documentation markers
        run: node scripts/tmp-patch-acquisition-registry-validator.mjs
      - name: Validate Acquisition Registry after documentation update
        run: node scripts/check-calendar-acquisition-registry.mjs
      - name: Validate diff formatting
        run: git diff --check
      - name: Commit documentation state and remove temporary updater
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            START-HERE.md \
            docs/project-roadmap.md \
            docs/calendar/implementation-roadmap.md \
            docs/calendar/acquisition-control-plane-implementation-plan.md \
            docs/calendar/machine-readable-contracts.md \
            docs/calendar/README.md \
            scripts/check-calendar-acquisition-registry.mjs
          git rm \
            .github/workflows/tmp-update-acquisition-registry-doc-state.yml \
            scripts/tmp-update-acquisition-registry-doc-state.mjs \
            scripts/tmp-patch-acquisition-registry-validator.mjs
          git diff --cached --check
          git commit -m "[CF-Pages-Skip] Advance Acquisition Registry state"
          git push origin HEAD:work/calendar-acquisition-registry
