name: v1 mobile QA gate

on:
  pull_request:
    branches:
      - main
    paths:
      - 'src/**'
      - 'data/static/v1-mobile-qa-v1.json'
      - 'data/audits/v1-mobile-qa-v1.json'
      - 'docs/release/v1-mobile-qa.md'
      - 'scripts/run-v1-mobile-qa-browser.mjs'
      - 'scripts/check-v1-mobile-qa.mjs'
      - '.github/workflows/v1-mobile-qa.yml'
  push:
    branches:
      - main
    paths:
      - 'src/**'
      - 'data/static/v1-mobile-qa-v1.json'
      - 'data/audits/v1-mobile-qa-v1.json'
      - 'docs/release/v1-mobile-qa.md'
      - 'scripts/run-v1-mobile-qa-browser.mjs'
      - 'scripts/check-v1-mobile-qa.mjs'
      - '.github/workflows/v1-mobile-qa.yml'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22.12.0
      - name: Install dependencies without generating a lockfile
        run: npm install --package-lock=false
      - name: Build complete static site
        run: npm run build
      - name: Preserve Phase 10 UX release
        run: node scripts/check-ux-polish-release.mjs
      - name: Preserve Phase 11 SEO release
        run: node scripts/check-seo-qa-release.mjs
      - name: Preserve v1 scope freeze
        run: node scripts/check-v1-scope-freeze.mjs
      - name: Preserve v1 data audit
        run: node scripts/check-v1-data-audit.mjs
      - name: Show available browser
        run: |
          command -v google-chrome-stable || command -v google-chrome || command -v chromium || command -v chromium-browser
      - name: Render every public page at mobile widths
        run: node scripts/run-v1-mobile-qa-browser.mjs
      - name: Normalize permanent report name
        run: mv v1-mobile-qa-discovery.json v1-mobile-qa-report.json
      - name: Validate v1 mobile QA contract and report
        run: node scripts/check-v1-mobile-qa.mjs
      - name: Upload mobile QA report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: v1-mobile-qa-report
          path: v1-mobile-qa-report.json
          if-no-files-found: error
          retention-days: 7
      - name: Remove generated report
        if: always()
        run: rm -f v1-mobile-qa-report.json v1-mobile-qa-discovery.json
      - name: Prove repository clean
        run: |
          changes="$(git status --porcelain --untracked-files=all)"
          if [ -n "$changes" ]; then
            printf '%s\n' "$changes"
            exit 1
          fi
