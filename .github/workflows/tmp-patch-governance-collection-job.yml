name: Temporary governance Collection Job patch

on:
  pull_request:
    paths:
      - 'scripts/tmp-patch-governance-collection-job.mjs'
      - '.github/workflows/tmp-patch-governance-collection-job.yml'
  push:
    branches:
      - work/calendar-collection-job-schema

permissions:
  contents: write

concurrency:
  group: tmp-patch-governance-collection-job
  cancel-in-progress: false

jobs:
  patch-and-clean:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: work/calendar-collection-job-schema
      - uses: actions/setup-node@v4
        with:
          node-version: 22.12.0
      - name: Patch governance validator
        run: node scripts/tmp-patch-governance-collection-job.mjs
      - name: Validate governance and Collection Job
        run: |
          node scripts/check-project-governance-docs.mjs
          node scripts/check-calendar-collection-job.mjs
      - name: Remove temporary patch files and commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scripts/check-project-governance-docs.mjs
          git rm \
            .github/workflows/tmp-patch-governance-collection-job.yml \
            scripts/tmp-patch-governance-collection-job.mjs
          git diff --cached --check
          git commit -m "[CF-Pages-Skip] Align governance with Collection Job state"
          git push origin HEAD:work/calendar-collection-job-schema
