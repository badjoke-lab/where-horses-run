Post-#913 UI density acceptance target:

- mobile home/list uses available width instead of nested horizontal gutters
- mobile home shows the meeting list before the map in document order
- mobile calendar shows the 30-day list before secondary map content in document order
- mobile hero, filters, date navigation, meeting groups, rows, header, and bottom nav use a denser spacing scale
- desktop explicit grid areas remain unchanged

Representative Visual Audit must be inspected before merge.
