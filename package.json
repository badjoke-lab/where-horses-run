{
  "name": "where-horses-run",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "validate:data": "node scripts/validate-data.mjs",
    "check": "npm run validate:data && npm run validate:fixtures && npm run build",
    "validate:fixtures": "node scripts/validate-parser-fixtures.mjs"
  },
  "dependencies": {
    "astro": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
